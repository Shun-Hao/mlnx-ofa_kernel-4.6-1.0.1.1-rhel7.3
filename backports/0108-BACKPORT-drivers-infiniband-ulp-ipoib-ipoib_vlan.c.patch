From: Talat Batheesh <talatb@mellanox.com>
Subject: [PATCH] BACKPORT: drivers/infiniband/ulp/ipoib/ipoib_vlan.c

Change-Id: Iaa3778210a8919cf697c2c3881a1e470409b79f8
---
 drivers/infiniband/ulp/ipoib/ipoib_vlan.c | 11 ++++++++++-
 1 file changed, 10 insertions(+), 1 deletion(-)

diff --git a/drivers/infiniband/ulp/ipoib/ipoib_vlan.c b/drivers/infiniband/ulp/ipoib/ipoib_vlan.c
index xxxxxxx..xxxxxxx 100644
--- a/drivers/infiniband/ulp/ipoib/ipoib_vlan.c
+++ b/drivers/infiniband/ulp/ipoib/ipoib_vlan.c
@@ -97,8 +97,9 @@ int __ipoib_vlan_add(struct ipoib_dev_priv *ppriv, struct ipoib_dev_priv *priv,
 	 * We do not need to touch priv if register_netdevice fails, so just
 	 * always use this flow.
 	 */
+#ifdef HAVE_NET_DEVICE_NEEDS_FREE_NETDEV
 	ndev->priv_destructor = ipoib_intf_free;
-
+#endif
 	/*
 	 * Racing with unregister of the parent must be prevented by the
 	 * caller.
@@ -114,6 +115,10 @@ int __ipoib_vlan_add(struct ipoib_dev_priv *ppriv, struct ipoib_dev_priv *priv,
 	priv->pkey = pkey;
 	priv->child_type = type;
 
+#ifndef HAVE_NDO_GET_IFLINK
+	priv->dev->iflink = ppriv->dev->ifindex;
+#endif
+
 	if (!is_child_unique(ppriv, priv)) {
 		result = -ENOTUNIQ;
 		goto out_early;
@@ -156,8 +161,10 @@ sysfs_failed:
 	return -ENOMEM;
 
 out_early:
+#ifdef HAVE_NET_DEVICE_NEEDS_FREE_NETDEV
 	if (ndev->priv_destructor)
 		ndev->priv_destructor(ndev);
+#endif
 	return result;
 }
 
@@ -266,7 +273,9 @@ static void ipoib_vlan_delete_task(struct work_struct *work)
 
 		ipoib_dbg(ppriv, "delete child vlan %s\n", dev->name);
 		unregister_netdevice(dev);
+#ifndef HAVE_NET_DEVICE_NEEDS_FREE_NETDEV
 		kfree(priv);
+#endif
 	}
 
 	rtnl_unlock();
